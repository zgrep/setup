#!/bin/sh

doas=/usr/bin/doas

if [ "$*" = '-i' ]; then
    $doas "$SHELL"
elif [ "$1" == '-c' ]; then
    shift
    $doas "$SHELL" -c "$*"
elif [ "$1" = '-e' ]; then
    shift
    $doas "$EDITOR" "$@"
else
    $doas "$@"
hfi
